# 🐕 RAM Watchdog

Monitor RAM usage of IDE-related processes directly in your editor.

## Features
- Live RAM display in status bar
- Color-coded severity (green → yellow → red)
- Click for detailed process breakdown
- Kill heavy processes from QuickPick menu
- Configurable thresholds and update interval
- Supports Windows, macOS, Linux
